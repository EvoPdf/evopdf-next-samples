{
  "$schema": "https://context7.com/schema/context7.json",
  "projectTitle": "EvoPdf Next samples",
  "description": "Runnable C# samples for EvoPdf Next (.NET PDF library): HTML, Word, Excel, RTF, Markdown to PDF, PDF to text, text search, PDF to image, image extraction, PDF editing, plus the complete ASP.NET Core demo application",
  "folders": [
    "quickstarts",
    "docs-samples"
  ],
  "excludeFolders": [
    "demo/AspNetDemo/wwwroot",
    ".github"
  ],
  "rules": [
    "Use the EvoPdf.Next namespace; reference one NuGet package per platform (EvoPdf.Next.Windows, EvoPdf.Next.Linux, EvoPdf.Next.MacOS)",
    "Set Licensing.LicenseKey once, statically, before the first conversion; never invent a key",
    "Converter instances are not reusable: create a new converter for every conversion",
    "API rules, migration guidance and skills for coding assistants live in https://github.com/EvoPdf/evopdf-agent-skills; consult that library for how to use the API, and this one for runnable code"
  ]
}
